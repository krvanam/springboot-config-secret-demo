apiVersion: v1
kind: Secret
metadata:
  name: spring-secret
type: Opaque
data:
  # Base64-encoded values for sensitive data. The values below correspond to
  # "admin" for DB_USER and "secret123" for DB_PASSWORD. When modifying these
  # values, remember to encode the new strings using base64.
  DB_USER: YWRtaW4=
  DB_PASSWORD: c2VjcmV0MTIz